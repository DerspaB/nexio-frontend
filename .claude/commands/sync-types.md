Revisa endpoints del backend y asegúrate de que existen:
- Interface en packages/types para cada response
- Schema Zod en packages/validations para cada input
- Función en packages/api-client para cada endpoint
Crea lo que falte.
